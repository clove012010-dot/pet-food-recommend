# 宠物粮推荐引擎 环境变量配置
# 复制此文件为 .env 并修改（不要提交 .env）

# CORS 白名单（逗号分隔），localhost 无论端口始终放行
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 速率限制 — 每 IP 在 WINDOW 毫秒内最多 REQ_MAX 次请求
RATE_LIMIT_MAX=30
RATE_LIMIT_WINDOW=10000

# 是否信任反向代理（启用后从 X-Forwarded-For 读取客户端 IP）
TRUST_PROXY=false
